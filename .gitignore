# Python
.venv*/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# Generated data (rebuilt by the pipeline / at runtime)
backend/app/data/*.db
# …but the prebuilt historical dataset MUST ship (deploys build from GitHub).
!backend/app/data/players.db

# Node / frontend
node_modules/
dist/
*.log
npm-debug.log*

# Env / secrets
.env
.env.*

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Scraped 2K roster pages (raw inputs to the rating pipeline; re-scrapeable,
# not source). Ignored wherever they land (repo root or pipeline/2k_pages/).
*2K26 Roster*.html
*.bak
